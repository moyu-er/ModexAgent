# 05 — Terminal trio + todo via roster (first vertical slice)

**What to build:** The terminal trio (`bash`/`process`/`terminal`) and the todo tool assemble entirely through the roster + factory path for one pool; the business glue dies. `use_terminal` now governs exactly one thing: whether the terminal manager (infrastructure) is built. Platform/backend selection (winpty/tmux/pexpect fallback ladder) stays inside the FW manager factory — it is real platform logic, not glue, and is not config-ized. The bash-without-backend degradation to `SubprocessTool` becomes factory-internal behavior, invisible to config. The ACI same-name replacement is resolved at compile time with an auditable record (`edit ← aci`). This ticket proves the design's load-bearing assumption end to end: a factory reading the context chain gets everything it needs.

**Blocked by:** 04 (factories read from the context chain).

**Status:** closed (resolved 2026-08-21)

- [x] One shipped pool's tools assemble via roster references (`tools: [bash, process, terminal]` + supplements) with the factories reading the chain — zero direct construction in the business glue for that pool
  Scope extended per plan Metis H2: the builders.py tool-construction segments are deleted GLOBALLY (legacy `assembly_spec is None` preset/supplement branch, `_make_bash`, the directly-constructed trio, `_make_shell_tool`), so every shipped pool's tools — the trio + todo included — assemble exclusively through the roster path. No shipped pool.yml/template change was needed: all four shipped pools run `use_terminal: false` (no trio declared, bash degrades via `BashToolFactory`), and todo/aci supplements already flow through `SpecBuilder` → TOOL-slot factories. `use_terminal` now governs only manager construction (the strategy calls the FW `create_terminal_manager_or_none`).
- [x] Split-brain: same pool config, old path (builders construction) vs new path (roster + factories) produce identical toolsets, verified tool-by-tool
  Baseline commit `4c857aac` froze the OLD road's products as goldens (`examples/bot_project/tests/unit/service/fixtures/split_brain_05/`): default-config toolset (legacy builders branch), trio construction (shared `ProcessRegistry`), and the full production `AssemblyManifest` (tools+hooks+memory config+prompt sha+provider+interceptors+commands+comm targets+terminal/todo infra+lazy subagent's compiled effective spec — observed products, not config echo). The migration compares the NEW road against the frozen goldens: one allowlisted difference (`bash` SubprocessTool timeout 90→300 — pre-existing dead-branch divergence; the roster value is the shipped production behavior since ADR-0041, no SPEC errata required), everything else item-for-item identical. Bare golden refresh is structurally impossible (tests assert manifest == golden; unlisted diffs turn red; allowlist entries self-verify against actual old/new values).
- [x] ACI split-brain: `tool_supplements: [aci]` produces the same effective edit tool as today's ToolManager last-write-wins, but with the replacement recorded and queryable at startup
  Behavioral half (per SPEC Errata-2): the effective edit tool is `AciEditTool` on both roads — old road via ToolManager last-write-wins overwrite, new road via `SpecBuilder`'s `edit → aci_edit` rename resolved through the registry (factory registration source visible in the manifest). The accounting-queryable half (boot-time replacement record) is deferred to ticket 06 per Errata-2, as planned.
- [x] `use_terminal: false` (review pool's current state): no manager built, tool references degrade exactly as today
  All shipped pools are `use_terminal: false`; the production manifest golden asserts `terminal_manager: null`, `bash → SubprocessTool`, and no `process`/`terminal` tools — identical before/after the migration. The FW factory availability contract is unchanged: process/terminal without a manager raise `ValueError` (never silent); bash degrades to `SubprocessTool`.
- [x] Deletion ledger honored: the builders.py tool-construction block and the BIZ-side trio fallback ladder are deleted
  Deleted: `_build_terminal_manager` (the whole BIZ ladder — migrated verbatim into FW `create_terminal_manager_or_none` next to `create_terminal_manager`, platform logic not config-ized), the todo path-injection segment (→ `build_pool_todo_store` supplied-infra helper called by the strategy; the store flows to `TodoToolFactory` via `pool_runtime.todo_store`), `_make_bash`, the legacy preset/supplement branch, the directly-constructed trio (the pool-unique `ProcessRegistry` is now created by `PoolAssembleStage`'s existing manager-without-registry invariant), and the dead `_make_shell_tool`. `grep -n "CommandTool\|ProcessTool\|TerminalTool\|SubprocessTool" examples/bot_project/bot/service/builders.py` → zero hits.
- [x] 1900 bot tests + T0.x slot gates green
  `python -m pytest examples/bot_project/tests -q` → 2097 passed, 6 skipped; `python scripts/verify_slot_gates.py` → 16/16; `python -m pytest tests/unit -q` → 8151 passed (baseline 8142 + 9 new: 6 FW ladder tests + 3 todo-roster tests, net of the ported BIZ ladder tests); `mypy src/modex_agent` → 93 errors (unchanged baseline, zero in touched files); ruff → zero findings on all changed files.
