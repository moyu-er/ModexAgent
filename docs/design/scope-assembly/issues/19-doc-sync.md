# 19 — Documentation status sync

**What to build:** After the waves land, the documentation reflects reality: `CONTEXT.md` terms (Scope / Scope Kind / Declaration Tree / Session Tree / Skeleton / Context Chain / Profile) drop their *Forthcoming* markers as their referents become live; the SPEC's status header moves from "设计定稿，未开始实施" to implemented status with per-wave errata appended (following the scope-converge Errata precedent — implementation findings fold back into the SPEC rather than living in scratch notes); ADR-0042/0043 cross-references verified (every "deliberately not" claim still true, every cited code anchor still exists).

**Blocked by:** 11 (legacy path gone — the design's promise is delivered), 15 (addressing converged), 17 (multi-workspace delivered — the last user-visible capability).

**Status:** closed (resolved 2026-08-22)

- [x] All seven CONTEXT.md terms updated: *Forthcoming* markers removed, definitions match the shipped implementation (five markers removed — Scope/Scope Kind/Declaration Tree/Context Chain/Profile; Session Tree needed none; Skeleton's `WorkspaceRegistry` anchor corrected to `ScopeRegistry` + dispatch-shell PoolRouter. Definitions now cite the real mechanisms: `ScopeSpec`/`load_scope_declaration`, two-phase validator + pure compiler, the `AgentContext` diamond, `ProfileStore`/`STANDARD_PROFILES` with position-bound toolset profiles. `grep "Forthcoming per ADR-0042" CONTEXT.md` → zero hits)
- [x] SPEC status header updated; implementation errata (if any) appended per the scope-converge Errata-1-8 precedent (header → 已实施 2026-08-22; §13 gained Errata-3~7: wave mapping vs executed waves (Metis M4), V10 phase attribution (Oracle#4 — phase-one table authoritative), §1.2 module count (Metis LOW — pool/ was 7 files at baseline, not 11), Profile storage face (`config/profiles/` did not land — `STANDARD_PROFILES` are code-level constants), and the 20-row ADR-0042/0043 anchor audit table)
- [x] ADR-0042/0043 anchor audit: every cited symbol/file exists as described; drift corrected in place (20 rows: 16 ✓, 4 fixed in place — PoolEditor write-back → scope declaration editor; `WorkspaceRegistry[R]` → `ScopeRegistry[R]` in Considered Options; the addressing Consequence rewritten to the delivered outcome (rename + `ScopePath`/`resolve_scope_path` + PoolRouter as dispatch shell with `agent_pool_ownership`); both ADRs gained implementation-status closing lines; known residue (comm_kind shim, dormant capacity) recorded, not hidden)
- [x] docs/AGENTS.md + docs/adr/AGENTS.md indexes current (scope-assembly entries marked implemented 2026-08-22 with SPEC/issues/closure-matrix key files; ADR index rows 0042/0043 annotated)
- [x] Closure matrix (docs/handoff/) annotated with wave-completion status — or moved into the design dir if it should persist (moved to `docs/design/scope-assembly/closure-matrix.md` — tracked location; implementation-status header records wave→ticket completion, finding resolutions, and the SPEC §13 errata pointer; handoff original preserved)
- [x] Root AGENTS.md module table updated if new framework modules were created (scope declaration/validation/compile) (root framework-areas line + `src/modex_agent/AGENTS.md` module table gained the `scope/` row; `src/modex_agent/scope/AGENTS.md` created following the plugins/AGENTS.md module-doc convention)

## Resolution notes

- Drift sweep beyond the six faces (the wave's code changes had doc-level fallout the checkbox list predates): `plugins/AGENTS.md` first-seen-wins → O2 source priority; `multi_agent/AGENTS.md` Phase-2 peer wiring → declared links + FW `peer_resolution.py`, deleted `workspace_paths.py` row, added `pool_router.py` row; `workspace/AGENTS.md` ScopeRegistry rename + `scope_path.py`; `bot/AGENTS.md` + `bot/service/AGENTS.md` framework relocations (pool_router/pool_instance), declaration-driven `pool/` (8 modules), dead `workspace.enabled`/bundle halves, MCP acquire path; `config/AGENTS.md` pools/ → scopes/; `bot_project/AGENTS.md` named stale items (pool_router row, 11-module claim, config/pools rows, Phase-2 registration, pool.yml wiring-chain, PoolEditor); `tests/AGENTS.md` + `tests/webui/AGENTS.md` renamed-class/single-home descriptions.
- Residual recorded in evidence: `examples/bot_project/README.md` still documents the deleted pool.yml format (user-facing pass is its own follow-up); test file names not renamed with `ScopeRegistryStore` (test renames out of this ticket's scope per MUST-NOT).
- Gates on a docs-only ticket (run to prove zero surprise): tests/unit 8010 passed / bot 2063 passed / slot gates 16/16.
