# 08 — BotAgentNode relaxation + V10 live

**What to build:** A graph node referencing a never-dispatched lazy agent works end to end: BotAgentNode's upfront instance resolution relaxes — instance absent → deliver anyway, the InboxPoller cold-starts the agent from its compiled declaration (the same lazy path as session-mode dispatch; the mechanism already exists). Node description resolution reads the compiled declaration instead of the live instance, and the pipeline pre-flight check goes with it. Per SPEC §4 axis 3, graph-referenced agents materialize on inbox consumption — same semantics as session mode, one path. Startup validation (V10, ticket 03) guarantees typo'd references never reach runtime.

**Blocked by:** 06 (compiled declarations carry the description and existence data).

**Status:** closed (resolved 2026-08-21)

- [x] A graph referencing a never-dispatched lazy leaf executes: deliver → poller cold-start materialization → turn runs → result flows back through the graph (`test_botagentnode_lazy_leaf.py`: `test_lazy_leaf_cold_start_turn_and_eviction_roundtrip` proves deliver-without-instance → `_materialize_then_turn` cold start → turn; `test_deliver_capable_node_result_flows_back` proves the deliver-tool → END → graph result chain through the same harness for deliver-capable NORMAL nodes. Note for ticket 12's E2E: a SUBAGENT-kind lazy leaf cannot itself deliver into the graph today — `GraphToolConfigurator`/`GraphTopologyConfigurator` gate on `agent_kind == NORMAL` (`turn_context_config.py`), and template-materialized agents are always SUBAGENT comm kind; lifting that gate is src-side machinery outside this ticket's lane)
- [x] Node descriptions resolve from the compiled declaration (fresh boot, no instances) — `resolve_description` falls back to the template registry (the boot-seeded runtime carrier of the 06 compilation; the same existence/materialization source the InboxPoller reads); unit tests in `test_agent_node_description.py` (cold start, empty-description sentinel, unknown agent sentinel)
- [x] The instance pre-flight RuntimeError path is gone (no earlier-than-execution failure for lazy agents); V10 covers misspelled names at startup instead — `grep "not found in pool" examples/bot_project/bot/graph/agent_node.py` zero hits; V10 boot failure covered by ticket 07's `test_boot_v10_missing_graph_agent_fails_loud` (referenced, not duplicated)
- [x] Graph turns and session turns for the same agent coexist without cross-talk (mode-neutral contract: instance products carry no mode state) — `test_graph_turn_and_session_turn_coexist` (session-mode turn via `submit_input` after a graph turn, no binding carry-over) + deliver-tool-not-in-instance tool-manager assertions on both the materialized leaf and the eager main agent (SPEC §4 axis 3 hard contract)
- [x] Integration test: review_cycle graph referencing a lazy subagent node, executed twice with eviction of the materialized instance in between — eviction via `WorkspaceRegistry.evict_and_release` direct call (capacity-based LRU dormant, D1); the fresh pool re-materializes the leaf from its template on the second run
- [x] Existing graph integration tests green — full bot suite: 2130 passed, 6 skipped (`test_cli.py::test_resolve_venv_python_candidates` fails on the clean c4195245 baseline in this lane's environment — main-tree venv on PATH; unrelated to this ticket, documented in the lane evidence)
