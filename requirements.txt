# ============================================================
# ModexAgent — Full dependency list (kept in sync with pyproject.toml)
#
# Install core only:
#   pip install -e .
#
# Install with all extras (recommended for bot_project):
#   pip install -e ".[all,dev]"
#   or
#   pip install -r requirements.txt
# ============================================================

# ---- Core dependencies ----
pydantic>=2.0.0,<3
filelock>=3.12.0
httpx>=0.24.0
pyyaml>=6.0.0
mcp>=1.0.0,<2
pathvalidate>=3.0.0
typing-extensions>=4.8.0
ddgs>=9.0.0
markdownify>=0.13.0
typer>=0.9.0

# ---- LLM support ----
litellm>=1.82.6
openai>=2.20.0,<3

# ---- Terminal backends (platform-specific) ----
pywinpty>=3.0; sys_platform == 'win32'
pexpect>=4.0; sys_platform != 'win32'
libtmux>=0.15.0; sys_platform != 'win32'

# ---- AST tools (tree-sitter, optional) ----
# Install: pip install -r requirements.txt
# Or: pip install ModexAgent[ast]
tree-sitter>=0.24
tree-sitter-python>=0.23
tree-sitter-java>=0.23

# ---- Sandbox backends ----
docker>=6.0.0
e2b-code-interpreter>=0.1.0

# ---- Gateway adapters (QQ Bot, env loading, HTTP) ----
qq-botpy>=1.2.0
python-dotenv>=1.0.0
aiohttp>=3.8.0
questionary>=2.0.0

# ---- Skills & document processing (bot_project subagents / media_utils) ----
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
Pillow>=10.0.0
defusedxml>=0.7.0
lxml>=4.9.0
pdf2image>=1.16.0
pdfplumber>=0.10.0

# ---- Development ----
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
ruff>=0.1.0
mypy>=1.0.0
